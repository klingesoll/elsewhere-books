# Elsewhere Books Website

Elsewhere Books 的官网项目，基于 React + Vite。

## Quick Start

```bash
npm install
npm run dev
```

默认地址：
- http://localhost:5173

## Scripts

- `npm run dev`：先拉取活动数据，再启动开发服务器
- `npm run build`：先拉取活动数据，再构建生产包
- `npm run preview`：本地预览生产包
- `npm run fetch-events`：手动同步 Luma 活动到 `public/events.json`

## Minimal Structure

- `src/components/`：页面区块组件
- `src/hooks/useLumaEvents.js`：前端读取 `events.json`
- `scripts/fetch-events.js`：从 Luma 拉取并标准化活动数据
- `public/events.json`：活动缓存文件
- `.github/workflows/fetch-events.yml`：定时同步活动数据

## How Data Works

数据流程：
1. `fetch-events.js` 请求 Luma API
2. 生成标准化活动数据
3. 写入 `public/events.json`
4. 页面通过 `useLumaEvents` 读取并渲染

失败兜底：
- 拉取失败且本地有 `events.json`：继续用缓存
- 拉取失败且无缓存：生成空数据，避免构建中断

## Deploy

- 部署平台：Vercel
- 分支：`main`
- 构建命令：`npm run build`
- 输出目录：`dist`

## Engineering Rules

- `main` 分支保持可部署
- 提交信息建议使用 Conventional Commits（`feat` / `fix` / `chore` / `docs`）
- PR 保持小而明确，描述变更影响和验证方式

## Pre-Merge Checklist

```bash
npm run dev
npm run build
```

确认：
- 页面可正常打开
- 活动数据正常显示
- 没有误提交文件

## Troubleshooting

`vite is not recognized`
- 先执行：`npm install`

`git push` 返回 403
- 当前 GitHub 账号没有仓库写权限，需要仓库所有者添加 collaborator